"use client";
export function VersionHistory({ sessionId }: { sessionId: string }) {
  // TODO: fetch GET /sessions/{sessionId}/versions and render snapshot list with rollback buttons
  return <p className="text-xs text-muted-foreground">TODO: versions for {sessionId}</p>;
}
